{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build oesgp_sin",
			"command": "cmake --build build --target oesgp_sin",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": [
				"$gcc"
			]
		}
	]
}